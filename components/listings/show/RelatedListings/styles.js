import styled from 'styled-components'
import {themeGet} from 'styled-system'
import Row from '@emcasa/ui-dom/components/Row'

export default styled(Row)`
  width: 100%;
  padding: ${themeGet('space.5')}px 0;
`

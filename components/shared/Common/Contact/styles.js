import styled from 'styled-components'

export default styled.div`
  h3 {
    font-size: 16px;
    font-weight: 700;
    margin: 10px;
  }

  textarea {
    resize: vertical;
    max-height: 180px;
  }
`
